version: "3.8"

services:
  blog:
    image: docker_blog
    ports:
      - 4000:4000
    container_name: jekyll-blog
    volumes:
      - ..:/srv/jekyll
    stdin_open: true
    tty: true
    command: jekyll serve -H 0.0.0.0 -t
